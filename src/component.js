const Document = require('./document')

class Component {
  print () {
    const result = ''
    return result
  }

  countWords () {
    if (component instanceof Document && component === 0) return 0
    //return 0
  }
}
module.exports = Component
